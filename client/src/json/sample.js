const { data } = require("./data.json");

const programData = data["BSIT"][0]["AREA1"];

console.log(programData);

// data["BSIT"]["AREA1"]["PARAMETERA"]["SYSTEM"].map((a) =>
//   console.log(`${Object.keys(a)[0]}:${a[Object.keys(a)[0]]}`)
// );
// console.log(data["BSIT"]["AREA1"]["PARAMETERA"]["SYSTEM"][0]);
